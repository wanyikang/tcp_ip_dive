19.1
先发送8个字节在发送12个字节的话，可能会引起延迟，应为nagle算法很有可能开启了，第一个8字节的报文发送出去后，下一个12字节的报文
必须要等第一个报文的ack到达后才能发送，因此产生了延迟。另外，若服务器实现了delayed
ack，则客户端要等待约200ms才能收到服务器的ack，两次发送的情况明显多一次200ms延迟。


19.2
pass


19.3
使用nagle算法的时间约534ms，禁止nagle算法的时间约为222ms。因为使用nagle算法时发送的是两个报文，禁止nagle时发送的是一个报文。延时不同。

